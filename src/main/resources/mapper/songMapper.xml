<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vcloud.domain.SongRepository">
<!-- 랜덤차트 10개 -->
	<select id="getTenRandomSongs" resultType="song">
		SELECT ID
			 , URL
			 , IMG_PATH
			 , SINGER
			 , TITLE
		  FROM vcloud.TB_DAY_CHART
		 ORDER BY RAND()
		 LIMIT 10
	</select>

<!-- 멜론차트 TOP30 -->
	<select id="getTopThirtySongs" resultType="song">
		SELECT ID
		     , RANK
		     , URL
		     , IMG_PATH
		     , SINGER
		     , TITLE
		     , LIVE
		  FROM vcloud.TB_DAY_CHART
		 ORDER BY RANK
		 LIMIT 30
	</select>
</mapper>